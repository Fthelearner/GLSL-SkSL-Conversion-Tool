// Generated from: /home/user/project/results/t5/tmp/pipeline_glsl_to_sksl_in/curve.frag
// OutputSkSL status: SUCCESS

// Generated by GLSL to SkSL Code Generator

// Fragment Shader

uniform float3 iResolution;
uniform float iTime;
uniform float3 iChannelResolution[4];

float map(float2 p) {
    float2 offset = float2(sin(p.g + cos(p.r)), cos(p.r + sin(p.g)));
    return (sin(p.r + offset.r) + cos(p.g + offset.g)) * 0.5;
}

half4 main(float2 fragCoord) {
    float4 fragColor;
    float2 uv = (fragCoord - 0.5 * iResolution.xy) / iResolution.g;
    float3 ro = float3(0.0, 3.0, iTime);
    float3 rd = normalize(float3(uv.r, uv.g - 0.5, 1.0));
    float t = 0.0;
    float3 p = float3(0.0, 0.0, 0.0);
    for (int i = 0; i < 100; i++) {
        p = ro + rd * t;
        float h = p.g - map(p.xz);
        if (h < 0.001 || t > 20.0) {
            break;
        }
        t += h * 0.5;
    }
    float3 col = float3(1.0, 1.0, 1.0);
    if (t < 20.0) {
        float height = p.g;
        float lines = fract(height * 10.0);
        float thickness = fwidth(height * 10.0) * 1.5;
        float lineMask = smoothstep(thickness, 0.0, lines);
        col = mix(float3(1.0, 1.0, 1.0), float3(0.0, 0.0, 0.0), lineMask);
        col = mix(col, float3(1.0, 1.0, 1.0), 1.0 - exp(-0.05 * t));
    }
    fragColor = float4(col, 1.0);
    return half4(fragColor.rgb, 1.0);
}


