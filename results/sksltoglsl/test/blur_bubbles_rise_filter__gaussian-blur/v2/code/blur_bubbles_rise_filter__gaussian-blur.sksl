// Generated from: /home/user/project/results/t5/tmp/pipeline_glsl_to_sksl_in_71248/blur_bubbles_rise_filter__gaussian-blur.frag
// OutputSkSL status: SUCCESS

// Generated by GLSL to SkSL Code Generator
// Fragment Shader
uniform shader image;
uniform float2 iResolution;
uniform float blurIntensity;
uniform float horizontal;
half4 main(float2 fragCoord) {
    float4 _skOut;
    float2 uv = fragCoord / iResolution;
    float2 texelSize = float2(1.0 / iResolution.r, 1.0 / iResolution.g);
    float2 direction = horizontal > 0.5 ? float2(1.0, 0.0) : float2(0.0, 1.0);
    float sigma = max(0.35, blurIntensity * 0.5);
    float4 centerColor = image.eval(uv * iResolution);
    float3 color = centerColor.xyz;
    float totalWeight = 1.0;
    for (int i = 1; i <= 10; ++i) {
        float fi = float(i);
        float sampleMask = step(fi, blurIntensity + 0.5);
        float weight = exp(-fi * fi / (2.0 * sigma * sigma)) * sampleMask;
        float2 offset = direction * texelSize * fi;
        color += image.eval((uv + offset) * iResolution).xyz * weight;
        color += image.eval((uv - offset) * iResolution).xyz * weight;
        totalWeight += 2.0 * weight;
    }
    _skOut = float4(color / max(totalWeight, 0.0001), centerColor.a);
    return half4(_skOut);
}

