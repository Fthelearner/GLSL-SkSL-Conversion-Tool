// Generated from: /home/user/project/results/t5/tmp/pipeline_glsl_to_sksl_in_62975/edge_light_shader_filter__gaussian-frag.frag
// OutputSkSL status: SUCCESS

// Generated by GLSL to SkSL Code Generator

// Fragment Shader

uniform shader image;
uniform float blurDirection;
uniform float sigma;

half4 main(float2 fragCoord) {
    float4 FragColor;
    float2 blur_direct = float2(1.0, 0.0);
    if (blurDirection > 0.5) {
        blur_direct = float2(0.0, 1.0);
    }
    float2 newFragCoord = float2(0.0, 0.0);
    float3 dest = float3(0.0, 0.0, 0.0);
    float w_sum = 0.0;
    for (int i = -2; i <= 2; i++) {
        float w = exp(-float(i) * float(i) / (2.0 * sigma * sigma)) / (sigma * 2.50662827);
        newFragCoord = fragCoord + float(i) * blur_direct;
        dest += w * image.eval(newFragCoord).xyz;
        w_sum += w;
    }
    dest /= w_sum;
    FragColor = float4(dest, 1.0);
    return half4(FragColor.rgb, 1.0);
}


