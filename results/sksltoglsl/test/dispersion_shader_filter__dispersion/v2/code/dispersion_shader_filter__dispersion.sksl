// Generated from: /home/user/project/results/t5/tmp/pipeline_glsl_to_sksl_in_72597/dispersion_shader_filter__dispersion.frag
// OutputSkSL status: SUCCESS

// Generated by GLSL to SkSL Code Generator
// Fragment Shader
uniform shader image;
uniform shader mask;
uniform float2 iResolution;
uniform float opacity;
uniform float2 redOffset;
uniform float2 greenOffset;
uniform float2 blueOffset;
half4 main(float2 fragCoord) {
    float4 _skOut;
    float2 fragUV = fragCoord / iResolution;
    float4 maskColor = mask.eval(fragCoord);
    float2 sdf = (maskColor.xy - 0.5) * 2.0;
    float alpha = maskColor.a * opacity;
    float3 dispersedColor = float3(0.0, 0.0, 0.0);
    float2 offset = redOffset * sdf;
    dispersedColor.r = image.eval(iResolution.xy * (fragUV + offset)).r;
    offset = greenOffset * sdf;
    dispersedColor.g = image.eval(iResolution.xy * (fragUV + offset)).g;
    offset = blueOffset * sdf;
    dispersedColor.b = image.eval(iResolution.xy * (fragUV + offset)).b;
    float4 imageColor = image.eval(fragCoord);
    float3 finalColor = mix(imageColor.xyz, dispersedColor, float3(alpha));
    _skOut = float4(finalColor, imageColor.a);
    return half4(_skOut);
}

