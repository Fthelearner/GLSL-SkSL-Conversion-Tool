// Generated from: /home/user/project/results/t5/tmp/pipeline_glsl_to_sksl_in_77564/sdf_from_image_filter__shader-string.frag
// OutputSkSL status: SUCCESS

// Generated by GLSL to SkSL Code Generator
// Fragment Shader
uniform shader imageInput;
uniform float spreadFactor;
const float SQRT_2 = 1.41421354;
half4 main(float2 fragCoord) {
    float4 _skOut;
    float2 centerFragCoord = fragCoord + float2(0.5, 0.5);
    float imageSample = imageInput.eval(centerFragCoord).a;
    if (imageSample <= 0.0) {
        return half4(float4(1.0, 1.0, 0.5, 0.5));
    }
    if (imageSample >= 1.0) {
        return half4(float4(0.5, 0.5, 1.0, 1.0));
    }
    float2 grad = float2(imageInput.eval(centerFragCoord + float2(0.5, 0.0)).a - imageInput.eval(centerFragCoord - float2(0.5, 0.0)).a, imageInput.eval(centerFragCoord + float2(0.0, 0.5)).a - imageInput.eval(centerFragCoord - float2(0.0, 0.5)).a);
    float2 normGrad = normalize(grad);
    float2 edgeCoords = float2(0.5, 0.5);
    float borderCoeff = (imageSample - 0.5) * 1.41421354;
    edgeCoords -= normGrad * borderCoeff / (2.0 * spreadFactor);
    if (borderCoeff < 0.0) {
        return half4(float4(edgeCoords, 0.5, 0.5));
    }
    _skOut = float4(0.5, 0.5, edgeCoords);
    return half4(_skOut);
}

