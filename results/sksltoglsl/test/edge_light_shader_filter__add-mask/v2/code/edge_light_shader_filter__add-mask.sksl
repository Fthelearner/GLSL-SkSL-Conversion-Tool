// Generated from: /home/user/project/results/t5/tmp/pipeline_glsl_to_sksl_in_61975/edge_light_shader_filter__add-mask.frag
// OutputSkSL status: SUCCESS

// Generated by GLSL to SkSL Code Generator

// Fragment Shader

uniform shader imageMask;
uniform shader image;
uniform shader imageBloom;
uniform float2 iResolution;
uniform float alphaProgress;

half4 main(float2 fragCoord) {
    float4 FragColor;
    float4 image_color = image.eval(fragCoord);
    float mask = imageMask.eval(fragCoord).a;
    float4 final_color = image_color;
    float4 dst = float4(1.0, 1.0, iResolution.r - 1.0, iResolution.g - 1.0);
    if (dst.r < fragCoord.r && fragCoord.r < dst.b && dst.g < fragCoord.g && fragCoord.g < dst.a && mask > 0.0) {
        final_color = float4(alphaProgress * imageBloom.eval(fragCoord).xyz * mask + image_color.xyz, image_color.a);
    }
    FragColor = final_color;
    return half4(FragColor.rgb, 1.0);
}


